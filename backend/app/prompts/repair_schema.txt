Convert the INPUT JSON below into a single valid JSON object matching the TARGET SCHEMA for the given CHART TYPE.

CHART TYPE: {{CHART_TYPE}}

TARGET SCHEMA:
{{TARGET_SCHEMA}}

RULES:
1. The response must contain ONLY the JSON object. No markdown, no code fences, no explanation, no comments.
2. The first character must be "{". The last character must be "}".
3. "labels" is required for all chart types EXCEPT scatter.
4. "datasets" is required. Each dataset has a "label" (string) and a "data" array.
5. For regular charts, "data" is an array of numbers.
6. For scatter charts, "data" is an array of objects with numeric "x" and "y" keys.
7. Remove any fields not in the target schema.
8. If the chart type is "pie" or "doughnut", keep only the first dataset.
9. Generate any missing required data points using sensible defaults (0 for numbers, "Unknown" for labels) when the input does not provide a value. Do NOT leave required fields empty.
10. Preserve all input data that already matches the schema; only transform or fill in what is necessary.

{{EXTRA_RULES}}

EXAMPLE for scatter input {"datasets":[{"label":"Samples","data":[{"x":"1"},{"x":"3"}]}]}:
{"datasets":[{"label":"Samples","data":[{"x":1,"y":0},{"x":3,"y":0}]}]}

After the JSON object, on a new line starting with "GENERATED:", list every data point you had to invent so the UI can show it to the user. Use one bullet per line. Example:
GENERATED:
- dataset 'Samples' point 0: y was generated as 0
- dataset 'Samples' point 1: y was generated as 0

After the GENERATED section, you may add one new line starting with "CHANGES:" listing short comma-separated changes. The CHANGES line is optional.

INPUT JSON:
{{INPUT_JSON}}

Return ONLY the repaired JSON object followed by the optional GENERATED and CHANGES sections. Do not write anything else.
